import { Circles } from 'react-loader-spinner';

const Loader = (
  <Circles
    height="500"
    width="500"
    color="#4fa94d"
    ariaLabel="circles-loading"
    wrapperStyle={{}}
    wrapperClass="Spiner"
    visible={true}
  />
);

export default Loader;
